titre: "Simple_Deref"
description: "Fonction de Création de référence polymorphe et utilisation"
prog: "let a x = ref x in let b = ((a  ((a 3) : (int ref))  ) : ((int ref) ref) ) in !b"
typeur: "(int ref)"
IO:
  - "ref ptr"
